userSession的重构已经完成。下一步，就是让userSession作为一个领域模型，从项目中分层出来，变为一个领域。
以上就是本次重构的全部核心目标。

与userSession相关的层有：
controller、role、Dispatcher.java。
视图层会比较特殊，它由控制层控制，并且仅AdminC.java控制器，就包括了大量的不同范围视图的交互。因此，为了解决这个问题，我决定将视图算作一个领域，单独算作一层。
它的分层将单独进行。
还包括statemachine、service、dto。
Dao层永远单独拎出来使用。
本领域没有特定的model。

战略：先整理分层，再重构。重构时，先解耦代码，然后，才是引入新的库，来帮我增强项目。最后一步（或者与上一步同时进行），才是修复bug。


本次重构的核心任务：对项目的model重构。
为了这次重构的方便，我首先需要梳理清楚，usersession涉及哪些内容。分析出来后，把与usersession相关的内容合并到一起。
要想完成usersession的分类，首先要在项目内，明确定义usersession的概念，讲明它应该负责什么事情，必需包含哪些内容，并定义这些内容如何互相协作。
因此，我将新开一个txt文件，专门用来解决这一概念争议。

已发现混乱根源。
原因：usersession混杂了认证逻辑、路由逻辑与菜单逻辑，造成混乱。
新目标：提取出菜单跳转逻辑，作为.core.menu层。随后，优化菜单代码。如果可以，一并提取出router逻辑。或者从菜单逻辑里拆分出来。