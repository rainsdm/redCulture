旧的重构计划彻底抛弃。现在要试图修正因为User表结构的修改，导致的大量隐性问题。这些问题无法通过编译、测试发现。只能手动运行寻找。目前，完全正常的部分，只有用户注册、登录的认证模块。

本次聚焦的bug类型是：表结构不一致问题。

测试完毕。除了发现了不稳定的数据库连接外，一个极其微弱的bug。

用户管理部分：（存在极微小的bug，不重要）
   注册用户 -> 正常
       省略部分信息。
       确认密码信息不一致 -> 正常，但是存在非预期输出。只影响使用，不重要。
       余下省略。